VITE_SANITY_PROJECT_ID=your_project_id_here
VITE_SANITY_DATASET=production
# Server-only write token, used by one-off Node migration scripts only.
# Never prefix this with VITE_ - that would let Vite inline it into the
# public browser bundle.
SANITY_API_TOKEN=your_api_token_here
